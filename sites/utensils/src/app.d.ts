declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		interface PageData {
			canonical: string;
			title: string;
			description: string;
		}
		// interface Platform {}
	}
}

export {};
